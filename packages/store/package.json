{
  "name": "@layai/store",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "bun build index.ts --outdir dist --target node --external @layai/utils --external @layai/types",
    "dev": "bun run tsc --watch",
    "lint": "eslint .",
    "type-check": "bun run tsc --noEmit"
  },
  "dependencies": {
    "zustand": "^4.4.0",
    "@layai/types": "workspace:*",
    "@layai/utils": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  }
}